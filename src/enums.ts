export enum FiltersValue {
  NO_STOPS = "no-stops",
  STOP_1 = "1-stop",
  STOP_2 = "2-stops",
  STOP_3 = "3-stops",
}

export enum SortingValue {
  FAST = "fast",
  CHEAP = "cheap",
  OPTIMAL = "optimal",
}
